# 📦 Stock Management System
## ระบบจัดการสต็อกสินค้า

ระบบจัดการสต็อกสินค้าที่สร้างด้วย Next.js พร้อมฟีเจอร์ครบครัน

### ✨ ฟีเจอร์หลัก

- **📋 จัดการข้อมูลสินค้า**: เลข MYOB, Model, Part Name, Part Number, Revision, เลข PO
- **📥 การรับเข้า**: วันที่รับเข้า, จำนวนที่รับเข้า
- **📤 การจ่ายออก**: เลข Invoice ออก, วันที่จ่ายออก, Due ที่ส่ง, จำนวนที่จ่าย
- **📝 หมายเหตุ**: ช่อง Remarks สำหรับข้อมูลเพิ่มเติม
- **📊 สรุปข้อมูล**: แสดงยอดรวมการรับเข้า จ่ายออก และคงเหลือ
- **🎨 UI สวยงาม**: ใช้ Tailwind CSS พร้อม Emoji และภาษาไทย

### 🛠️ เทคโนโลยีที่ใช้

- **Next.js 14** - React Framework with App Router
- **TypeScript** - Type Safety
- **Tailwind CSS** - Styling
- **Prisma** - Database ORM
- **SQLite** - Database

### 🚀 การใช้งาน

#### วิธีที่ 1: ใช้ Batch File (แนะนำ)
1. Double-click ไฟล์ `start-stock-system.bat`
2. รอระบบเริ่มต้น (ประมาณ 10-15 วินาที)
3. Browser จะเปิดอัตโนมัติที่ http://localhost:3000

#### วิธีที่ 2: สร้าง Desktop Shortcut
1. Double-click ไฟล์ `create-shortcut.vbs`
2. Shortcut จะถูกสร้างบน Desktop
3. Double-click shortcut เพื่อเริ่มใช้งาน

#### วิธีที่ 3: Command Line
```bash
npm install
npx prisma generate
npx prisma migrate dev --name init
npx prisma db seed
npm run dev
```

### 📁 โครงสร้างโปรเจค

```
stock-management-system/
├── app/                    # Next.js App Router
│   ├── api/stock/         # API Routes
│   ├── globals.css        # Global Styles
│   ├── layout.tsx         # Root Layout
│   └── page.tsx           # Home Page
├── components/            # React Components
│   ├── StockForm.tsx      # Form Component
│   └── StockTable.tsx     # Table Component
├── prisma/               # Database
│   ├── schema.prisma     # Database Schema
│   └── seed.ts           # Seed Data
├── types/                # TypeScript Types
│   └── stock.ts          # Stock Item Types
├── start-stock-system.bat # Quick Start Script
└── create-shortcut.vbs   # Shortcut Creator
```

### 📊 ข้อมูลตัวอย่าง

ระบบมาพร้อมข้อมูลตัวอย่าง 3 รายการ:
- Bearing Assembly (MYOB001)
- Motor Shaft (MYOB002) 
- Control Panel (MYOB003)

### 🔧 การพัฒนาต่อ

#### เพิ่มฟีเจอร์ใหม่
- การค้นหาและกรองข้อมูล
- การ Export ข้อมูลเป็น Excel/PDF
- การแจ้งเตือนเมื่อสต็อกต่ำ
- การสำรองข้อมูล

#### การเปลี่ยนฐานข้อมูล
แก้ไขไฟล์ `.env`:
```
DATABASE_URL="postgresql://user:password@localhost:5432/stockdb"
```

### 📞 การสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console ใน Browser (F12)
2. ตรวจสอบ Terminal ที่รันคำสั่ง
3. ตรวจสอบไฟล์ log ในโฟลเดอร์โปรเจค

### 📝 หมายเหตุ

- ข้อมูลจะถูกเก็บในไฟล์ `prisma/dev.db`
- สำรองข้อมูลเป็นประจำเพื่อความปลอดภัย
- ระบบรองรับการใช้งานหลายผู้ใช้พร้อมกัน

---
**🎯 พร้อมใช้งาน!** Double-click `start-stock-system.bat` เพื่อเริ่มต้น
